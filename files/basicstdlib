exit ! in code do
	60 code syscall1
	_
end

write ! in buf count do
	1 1 buf count syscall3
	_
end

;strlen in buf do
;    buf
;    while @ * 0 != do
;		++
;    end
;	buf -=
;end

main in do
	"Hello!\n" 7 write

	0 exit ; called anyway
end