include 'std.stas'

fn main 0 0 {
	"add.stas" drop O_RDONLY 0 open

	dup 0 <s if {
		"Failed to open file!\n" eputs
		1 exit
	}

	close 0 <s if {
		"Failed to close file descriptor!\n" eputs
		1 exit
	}
}