declare hello_str "Hello!\n" ; immutable

global string_ptr 0 ; mutable variable
; cannot contain literal strings, as editing
; them is pointless and undefined behavior

pop hello_str

push hello_str pop string_ptr 
	; move char pointer into u64

push string_ptr print
	; push pointer onto stack and print