include <std.stas>

fn main {
	1
	while dup 100 <= {
		dup 15 % 0 = if {
			"FizzBuzz\n" puts
		} elif dup 3 % 0 = {
			"Fizz\n" puts
		} elif dup 5 % 0 = {
			"Buzz\n" puts
		} else {
			dup putuln
		}
		++
	}
	drop
}